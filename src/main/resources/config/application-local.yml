spring:
  mongodb:
    uri: ${MONGODB_URI}

  security:
    user:
      name: admin
      password: admin123

security:
    jwt:
      secret: ${JWT_SECRET}

service:
  apiKey: ${API_KEY}

server:
  port: 8080